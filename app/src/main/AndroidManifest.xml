<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:activity="http://schemas.android.com/apk/distribution"
    xmlns:activityandroid="http://schemas.android.com/apk/res-auto"
    package="com.example.foodsafetyappl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/preview5"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_activity"></activity>
        <activity android:name=".foodalertrecyclerview_activity" />
        <activity android:name=".Consumer_activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".listviewactivity" />
        <activity android:name=".View_PDF_Files" />
        <activity android:name=".MyRecyclerViewActivity" />
        <activity android:name=".uploadpdf_activity" />
        <activity android:name=".products_activity" />
        <activity android:name=".recyclerview" />
        <activity android:name=".loginActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".modules_activity" />
        <activity android:name=".Signup_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>